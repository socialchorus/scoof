Scoof.subscriptions = {
  // show flash messages when an event is received from the server, or client
  flash: function (data) {
    var flasher = new Flasher(data);
    flasher.show();
  },

  // redirect when an event is generated by the server, or client
  redirect: function (href) {
    window.location = href;
  }
};
